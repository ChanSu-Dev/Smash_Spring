<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.java.smash.dao.IConnectionDao">
	<select id="listDao" resultType="com.java.smash.dto.DeviceDto">
		SELECT * FROM DEVICE
	</select>
	
	<update id="connectionStartDao">
		UPDATE DEVICE SET patientNumber = #{param1}, activated = 1 WHERE deviceNumber = #{param2}
	</update>
	
	<update id="connectionStopDao">
		UPDATE DEVICE SET patientNumber = NULL, activated = 0 WHERE deviceNumber = #{param1}
	</update>
</mapper>
